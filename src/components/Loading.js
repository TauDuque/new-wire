import React from "react";
import { Wrapper } from "../styles/LoadingStyles";

const Loading = () => {
  return (
    <Wrapper>
      <div className="loading"></div>
    </Wrapper>
  );
};

export default Loading;
